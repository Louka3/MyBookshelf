export const bookController = {
  // add book to users library
  // update book data in users library
  // delete book from users library
  // get book from users library
  // get full list of books from users library
  // search for a book using api
  // search for books using some sort of identifier (most likely only the title)
};
